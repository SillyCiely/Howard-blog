const mongoose = require('mongoose')
const blogpostsSchema = require('./blogposts-schema.js')

// schema: postId, postTitle, authorId, dateStamp, category, postBody,
//         image, imageMimeType, likes, usersLikes


module.exports = {
}